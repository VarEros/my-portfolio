export default class Project {
    constructor(image, name, title, description) {
      this.image = image;
      this.name = name;
      this.title = title;
      this.description = description;
    }
  }